<ul class="media-list">
  <% like_microposts.each do |like_microposts| %>
    <li class="media">
      <div class="media-left">
        <img class="media-object img-rounded" src="<%= gravatar_url(like_microposts.user, options = { size: 50 }) %>" alt="">
      </div>
      <div class="media-body">
        <div>
          <%= link_to like_microposts.user.name, user_path(like_microposts.user) %> <span class="text-muted">posted at <%= like_microposts.created_at %></span>
        </div>
        <div>
          <p><%= like_microposts.content %></p>
        </div>
      </div>
    </li>
  <% end %>
  <%= paginate like_microposts %>
</ul>